import { IconProps } from "react-feather"

export type ContactMeLinkProps = {
    brand: string
    link: string
    icon: IconProps
}

export type EducationItemProps = {
    name: string
    location: string
    level: string
    dates: string
    degrees: string
    acheivements: string
    activities?: string
}

